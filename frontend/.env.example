# URL of the deployed backend API. Defaults to http://localhost:3001 in code
# if unset, so this file is only needed when the backend runs somewhere else
# (e.g. a Railway deployment).
VITE_API_URL="http://localhost:3001"
